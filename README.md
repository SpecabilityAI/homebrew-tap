# Specability Homebrew Tap

Install Specability Core with Homebrew:

```bash
brew tap SpecabilityAI/tap
brew install specability
```

This tap distributes Specability Core binaries from
`SpecabilityAI/specability-core` GitHub Releases. It does not contain the
complete Specability product source or private method library.
